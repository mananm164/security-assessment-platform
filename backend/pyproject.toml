[tool.ruff]
target-version = "py312"
line-length = 120
extend-exclude = [
  "*/migrations/*.py",
  "*/tests/*.py",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
