import { Alert, Button, Card, CardContent, Divider, Link, Stack, Typography } from '@mui/material';

export default function RemediationDraftPanel({ artifacts, loading, error, canEdit, onGenerate }) {
  const latest = artifacts?.[0];
  return <Card><CardContent><Stack spacing={2}><Stack direction={{ xs: 'column', sm: 'row' }} spacing={1} justifyContent="space-between"><div><Typography variant="h6">AI-assisted remediation</Typography><Typography color="text.secondary">Draft — human review required</Typography></div>{canEdit ? <Button variant="outlined" onClick={onGenerate} disabled={loading}>{loading ? 'Generating...' : 'Generate draft'}</Button> : null}</Stack>{error ? <Alert severity="error">{error}</Alert> : null}{latest ? <><Typography component="pre" sx={{ whiteSpace: 'pre-wrap', fontFamily: 'inherit', m: 0 }}>{latest.content}</Typography><Divider /><div><Typography variant="subtitle2" sx={{ mb: 1 }}>Sources used</Typography><Stack component="ol" spacing={1} sx={{ pl: 3, m: 0 }}>{latest.sources?.map((source) => <li key={`${latest.id}-${source.rank}`}><Typography><Link href={source.source_url} target="_blank" rel="noreferrer">{source.title}</Link></Typography><Typography variant="body2" color="text.secondary">{source.source_name}</Typography></li>)}</Stack></div></> : <Typography color="text.secondary">No remediation draft has been generated for this finding yet.</Typography>}</Stack></CardContent></Card>;
}
